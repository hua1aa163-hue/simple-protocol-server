# v0.3.0 实现计划与验收边界

## 已完成

1. 采用本机 TCP 服务端监听，MRTEST/测量设备连接本程序；默认监听 `127.0.0.1:9527`。
2. 手动单次测量固定发送 `&|Meas|A|M|@`；设备开始测量后返回 `&|Meas|A|M|Run|@`（中间态），最终 `&|Meas|A|OK|@` 才完成；NG、断线、取消、600 秒超时终止当前计划。旧配置中误保存的带 `M|Run` 请求会自动迁移。
3. 主界面和测试项目编辑子界面使用 WinForms Designer 文件；路径、命令、轮数、项目次数和弹窗参数退出持久化。
4. 绑定项目→MRTEST 配方名/配方文件→固定图卡队列；配方管理器支持鼠标选择实际文件、图卡和小图预览，绑定路径与发送给 MRTEST 的名称分开保存。FOV/对比度/色域按 MRTEST 确认窗口顺序驱动投图，不依赖窗口类名或提示文字；串扰勾选“弹窗序列”时使用独立的逐图确认事务。
5. 串扰按参考项目保留多图、最后本底、起点循环、逐图最终 OK、导出快照/稳定性检查、Brightness 合并、19×32 统计及 Excel/CSV/PNG 输出。弹窗模式下每张图均按“投影→发送→Run→确认/关闭→OK”推进；每次重复分配独立结果目录，并在主界面提供结果批次选择。
6. 项目编辑器删除按绑定对象处理并只显示一次确认框；重绑期间屏蔽 DataGridView 重入，图卡路径、标签和预览同步刷新。
7. FOV、黑白对比度、亮度均匀性、色域和畸变已接入纯 C# 结果规则引擎；规则递归选择 ExportFile 最深层明细工作簿，主界面可编辑规则并在同一项目重复时横向追加第 N 次结果。
8. 主界面支持多个命名一键测试计划，可保存/另存/删除并通过下拉框切换；计划快照包含项目、图卡、重复次数、弹窗/投影参数、串扰分析参数和展示规则。

## 首测步骤

- 先只启用一个已经有图卡和 MRTEST 配方的项目。
- 启动 MRTEST/设备并让其连接 `127.0.0.1:9527`，确认主界面显示客户端已连接。
- 先运行 1 轮、项目 1 次；核对 Run→OK、图卡顺序、弹窗点击和 ExportFile 输出。
- 再逐项增加项目次数或整套轮数。

## 下一轮待确认

- 现场若 MRTEST 版本的色域或畸变工作表地址不同，可在“数据展示规则”编辑器中直接调整；当前实测色域明细使用 H3/H4、B23/C23（用户提出的 C24/C13/C16 位置保留为可编辑待现场确认）。
- 最终结果表/报告列顺序、单位、精度、文件命名，以及是否将 Excel 名称单元格直接作为展示名称（清空规则的“输出列”即可切换）。
- 最终结果表/报告列顺序、单位、精度、文件命名。
- 现场首测需确认 MRTEST 的确认窗口是否会作为主窗口内部子窗体出现；当前监视器已覆盖顶层拥有窗口和主窗口禁用时的活动子窗体，并按按钮→Enter→安全关闭回退。监视器在每条测量命令窗口启动/首次发现时绑定 PID，运行期间不切换到其他同名实例。串扰弹窗模式还会等待该命令的 Run 中间返回后再自动确认。
- MRTEST 位于 Program Files 且现场进程可能为高完整性级别；客户端清单使用 `requireAdministrator`，以跨过 Windows UIPI 发送 BM_CLICK、IDOK、Enter 和 WM_CLOSE。若未允许 UAC，监视器可看到窗口但不能驱动它。
